/**
 * Global Teardown
 * 
 * Cleanup after all tests complete
 */

export default async function globalTeardown() {
  // Cleanup any resources that persist across test runs
  // For example: close database connections, stop servers, etc.
}
